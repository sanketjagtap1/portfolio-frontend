// Production environment.
// IMPORTANT: point apiBaseUrl at your deployed backend before going live.
export const environment = {
  production: true,
  // Portfolio/blog/admin REST API.
  apiBaseUrl: 'https://api.your-domain.com',
  // Base URL where the backend serves uploaded images (the /uploads static folder).
  fileApiUrl: 'https://api.your-domain.com/uploads',
};
